apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: default
  labels:
    app: erp-tesseris-chat
spec:
  type: ClusterIP
  selector:
    app: erp-tesseris-chat
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
      name: http 